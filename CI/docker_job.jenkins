pipeline{
    agent any

    stages{
        stage("Docker build"){
            steps{
                echo "========executing Docker build========"
                sh '''
                    cd jenkisn-test
                    docker build -t jenkins-build -f CI/
                    docker run jenkins-build 
                '''
            }
        }
    }
}