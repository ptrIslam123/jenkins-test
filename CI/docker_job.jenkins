pipeline{
    agent {
        dockerfile {
            filename 'docker_job.jenkins'
            dir './'
            label 'master'
        }
    }

    stages{
        stage("Docker build"){
            steps{
                echo "========executing Docker build========"
            }
        }
    }
}